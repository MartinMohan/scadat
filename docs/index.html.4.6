<html><body><b><a href="https://pypi.python.org/pypi/scadat">scadat</a></b>
is a linux command line interface to the open source scada
<a href="https://en.wikipedia.org/wiki/TANGO" target="_blank">
<img src="tango_logo.jpg.png" alt="Tango_logo" height="42" width="42"></a>

<p><b>For help:</b> scadat -h</p>
<head><title>scadat</title></head>
<pre>
Usage: [-h] [-v] [-t T] [ -l | -r R [R ...] | -a A [A] | -c C [C] | -p P [P]]

Linux command line interface to tango

optional arguments:
  -h            show this help message and exit
  -v            increase verbosity using -v,-vv,-vvv
  -t T          Set TANGO_HOST (default=127.0.0.1:10000)
  -l            List tango devices
  -r R [R ...]  Read attributes,commands properties for multiple devices: e.g.
                scadat -r sys/tg_test/1 test/simulator/rampx2
                scadat -v -r sys/tg_test/1 test/simulator/rampx2
  -a A [A]      Read/Write to attributes of a device: e.g.
                scadat -a sys/tg_test/1
                scadat -a sys/tg_test/1/string_scalar 'hello world'
  -c C [C]      Read/Send commands to a device: e.g.
                scadat -c sys/tg_test/1
                scadat -c sys/tg_test/1/DevLong 1234
  -p P [P]      Read/Write properties to a device: e.g.
                scadat -p test/simulator/rampx2
                scadat -p test/simulator/rampx2/DeviceNr 2


Install on debian or ubuntu
---------------------------
1. Install <a href="https://pypi.python.org/pypi/PyTango">PyTango</a>
sudo apt-get install python-pytango
2. Install <a href="https://pypi.python.org/pypi/scadat">scadat</a>
sudo pip install scadat or
sudo pip install https\://pypi.python.org/packages/source/s/scadat/scadat-0.3.2.tar.gz
sudo pip install --no-deps -I 'scadat==0.3.2'

Testing
-------
scadat_test.sh will run unit tests.
scadat has been tested on debian and ubuntu but should run on other os

Notes
-----
1. TANGO_HOST default=127.0.0.1:1000. Override default by command line -t or export TANGO_HOST
2. A script scadat_search.sh can be used to search for tango systems using ip addresses. netcat (<a href="http://www.openbsd.org/cgi-bin/man.cgi/OpenBSD-current/man1/nc.1?query=nc"/>nc</a>) must be installed.
3. scadat can be used alone or in combination with unix tools like grep ,sort 
    scadat -r sys/tg_test/1 | sort
    scadat -r sys/tg_test/1 | grep string
    scadat -l | xargs scadat -v -r | grep Status # Get status of all devices
    scadat -l 2>errors.txt | xargs scadat -v -r | grep Status 2>> errors.txt | grep Status # Get status of all user devices and redirect stderr to errors.txt
4. Use <a href="http://scadat.org/regexf.html">regexf</a> to define a tango naming convention. Then use scadat to check if names are correct .e.g. scadat -l | xargs regexf -v
5. Properties registered using jive do not appear if registered using default property (Tango ticket)
6. contact martinmohan(at)yahoo.com to report bugs

CHANGES.txt
-----------
v<version>, <date>
0.1.5 (30/05/2014) For simpicity remove epics support and inheritance.
0.2.0 (19/11/2014) Format change test/simulator/ramp Amplitude -> test/simulator/ramp/Amplitude
0.2.3 (09/12/2014) Add scadat_unittests -> assertEqual,assertGrep, assertTrue, assertFalse
0.2.4 (03/01/2015) errors sent to stderr. It Starter not found list all devices
0.3.0 (25/03/2015) beta release, use only short form arguments
0.3.2 (21/04/2015) set default TANGO_HOST=localhost:10000, modified scadat -l and scadat -v, scadat_test.sh
0.4.0 (17/11/2015) -r reads properties, attributes and commands. -t sets TANGO_HOST
0.4.1 (09/12/2015) Tested with python3.4 and python2.7
0.4.2 (28/12/2015) scripts #!/bin/bash->#!/bin/sh (better posix compliance)
0.4.6 (28/01/2016) Update help

</pre>
</body></html>
