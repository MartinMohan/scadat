chmod u+x *.sh
mkdir assertbash-0.1
SOURCEBIN1=assertbash.sh
SOURCEBIN2=assertequal.sh
SOURCEBIN5=assertgrep.sh
SOURCEBIN3=assertfalse.sh
SOURCEBIN4=asserttrue.sh

cp *.sh assertbash-0.1/

cd assertbash-0.1/
dh_make -s --indep --createorig --email martinmohan@yahoo.com
grep -v makefile debian/rules > debian/rules.new
mv debian/rules.new debian/rules

echo $SOURCEBIN1 usr/bin > debian/install 
echo $SOURCEBIN2 usr/bin >> debian/install 
echo $SOURCEBIN3 usr/bin >> debian/install 
echo $SOURCEBIN4 usr/bin >> debian/install 
echo $SOURCEBIN5 usr/bin >> debian/install 

#echo assertbash usr/bin > debian/install
echo "1.0" > debian/source/format
rm debian/*.ex
debuild -us -uc
cd ..
sudo dpkg -i assertbash_0.1-1_all.deb 
