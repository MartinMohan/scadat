15/04/2015:
Rewrite in cpp?
replace -a,-c,-p with -w

Replace stdout with string - only call stdout once
write_attibute w_val and value

>>> import scadat
>>> myscadat=scadat.scadat_tango.scadat_tango()
>>> help(myscadat)
>>> myscadat.ldev()

Rather than get_starter_devices. 
just read device "tango/admin/cmpl71/RunningServers"
